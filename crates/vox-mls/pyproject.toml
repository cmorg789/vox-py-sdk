[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "vox-mls"
version = "0.1.0"
description = "Vox MLS encryption engine (Rust native extension)"
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
